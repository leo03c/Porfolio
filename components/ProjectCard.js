import React from 'react';\nimport { useLang } from '../lib/i18n';\n\nexport default function ProjectCard({ project }) {\n  const { t } = useLang();\n  return (\n    <article className="card hover:shadow-md transition-shadow">\n      <div className="flex items-start justify-between gap-4">\n        <div>\n          <h4 className="font-semibold text-lg">{project.title}</h4>\n          <p className="text-sm text-slate-600 mt-2">{project.short}</p>\n\n          <div className="mt-4 flex flex-wrap gap-2">\n            {project.tags?.map((tag) => (\n              <span key={tag} className="text-xs px-2 py-1 bg-slate-100 rounded-full text-slate-600">{tag}</span>\n            ))} \n          </div>\n        </div>\n\n        <div className="flex flex-col items-end gap-2">\n          <a href={project.repo} target="_blank" rel="noopener noreferrer" className="text-sm text-accent font-medium">{t.viewRepo}</a>\n          <a href={project.demo} target="_blank" rel="noopener noreferrer" className="text-sm btn-outline">{t.seeMore}</a>\n        </div>\n      </div>\n    </article>\n  );\n}