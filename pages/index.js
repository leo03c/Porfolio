import Head from 'next/head';\nimport Header from '../components/Header';\nimport Hero from '../components/Hero';\nimport ProjectCard from '../components/ProjectCard';\nimport projects from '../data/projects.json';\nimport { useLang } from '../lib/i18n';\n\nexport default function Home() {\n  const { t } = useLang();\n  return (\n    <>\n      <Head>\n        <title>{t.name} — {t.role}</title>\n        <meta name="description" content={t.heroPitch} />\n        <meta property="og:title" content={`${t.name} — ${t.role}`} />\n        <meta property="og:description" content={t.heroPitch} />\n      </Head>\n\n      <div className="min-h-screen flex flex-col">\n        <Header />\n\n        <main className="flex-1 max-w-6xl mx-auto px-4 py-12">\n          <Hero />\n\n          <section id="projects" className="mb-12">\n            <h3 className="text-2xl font-semibold mb-6">{t.projectsTitle}</h3>\n            <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">\n              {projects.map((p) => (\n                <ProjectCard key={p.id} project={p} />\n              ))} \n            </div>\n          </section>\n\n          <section id="contact" className="mb-12">\n            <h3 className="text-2xl font-semibold mb-4">{t.contactTitle}</h3>\n            <p className="text-sm text-slate-700">Email: <a href="mailto:leonardo@example.com" className="text-accent">leonardo@example.com</a></p>\n          </section>\n        </main>\n\        <footer className="py-6 border-t">\n          <div className="max-w-6xl mx-auto px-4 text-sm text-slate-500">© {new Date().getFullYear()} Leonardo Castillo Díaz — Desarrollador Fullstack</div>\n        </footer>\n      </div>\n    </>\n  );\n}